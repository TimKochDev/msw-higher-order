{
  "name": "msw-higher-order",
  "private": false,
  "version": "0.1.0",
  "description": "Higher-order functions for MSW request handlers",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "test": "vitest"
  },
  "peerDependencies": {
    "msw": "^2.0.0"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "~5.6.2",
    "vitest": "^2.1.8"
  }
}
